@model VideoGame

@*<div class="collapse" id="collapseExample @Model.Title">
    <div class="card card-body">
        <form asp-action="Loan" method="post">

            <input type="text" placeholder="Loaner Name" />
            <input type="date" placeholder="Date" />
            <button type="submit"></button>
        </form>
    </div>
</div>*@
<div class="container">

    <div class="row">
        <div class="col-sm-2">
            <img src="~/@Model.Image">
        </div>
        <div class="col-sm">
            <h3>@Model.Title</h3>
        </div>
        <div class="col-sm">
            <h3>@Model.Platform</h3>
        </div>
        <div class="col-sm">
            <h3>@Model.Genre</h3>
        </div>
        <div class="col-sm-1">
            <h3>@Model.ESRB</h3>
        </div>
        <div class="col-sm-1">
            <h3>@Model.Year</h3>
        </div>
        <div class="col-sm">
            <form asp-action="Loan" method="post">
                <input class="btn btn-primary" type="submit" value="Loan"/>
            </form>

            
        </div>
    </div>
</div>
